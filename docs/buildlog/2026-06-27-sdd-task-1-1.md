# sdd-task-1-1 (build log): Réorganiser tokens.css en couches + échelle d'espacement

**Date:** 2026-06-27

**Spec:** `/Users/pleguern/Workspace/breves-ia/.superpowers/sdd/task-1.1-brief.md`

**Plan:** TDD strict (write test → RED → code → GREEN → commit)

**Objectif:** Restructure `src/renderer/styles/tokens.css` into commented semantic layers (Primitifs, Sémantiques, Formes & typo) and add `--space-1..6` spacing scale, maintaining 100% visual parity on existing tokens.

**Statut:** ✅ Livré

---

## Livré

| Livrable | Fichier | Notes |
|----------|---------|-------|
| Test de garde (escale espacement + non-régression) | `tests/renderer/tokens.test.mjs` | Valide présence --space-1..6 + régression sémantiques (accent, good, warn, etc.) |
| Tokens restructurés + spacing scale | `src/renderer/styles/tokens.css` | Couches commentées ; --space-1:4px..--space-6:22px ; parité 100% existants |

---

## Validation RÉELLE

### Step 1: Test initial (RED expected)
```bash
npx vitest run tests/renderer/tokens.test.mjs
```

**Output (FAIL):** `AssertionError: --space-1 manquant` ✅
- 1 test failed: "échelle d'espacement présente"
- 1 test passed: "tokens sémantiques conservés (non-régression)"
- Confirmed --space-* tokens absent before code change

### Step 2: Code implementation (tokens.css restructured)
- Wrapped existing color/surface tokens under `/* ===== Primitifs : surfaces & couleurs ===== */`
- Wrapped semantic tokens under `/* ===== Sémantiques : intentions ===== */`
- Added new section: `/* ===== Échelle d'espacement ===== */` with `--space-1:4px; --space-2:8px; --space-3:11px; --space-4:14px; --space-5:18px; --space-6:22px;`
- Wrapped typography/radius under `/* ===== Formes & typo ===== */`
- Left `body.dark` block unchanged

### Step 3: Test pass (GREEN expected)
```bash
npx vitest run tests/renderer/tokens.test.mjs
```

**Output (PASS):** ✅
```
 Test Files  1 passed (1)
      Tests  2 passed (2)
```
- Both tests now pass: spacing scale present, semantic tokens retained

### Step 4: Typecheck
```bash
npx tsc --noEmit
```

**Output (PASS):** ✅ No TypeScript errors

### Step 5: Full suite (pre-commit hook)
```bash
git commit -m "refactor(tokens): couches sémantiques + échelle --space, garde de non-régression"
```

**Output (PASS):** ✅
```
> breves-ia-companion@0.1.0 typecheck
> node scripts/typecheck.mjs
[OK]

> breves-ia-companion@0.1.0 lint
> eslint .
[OK]

> breves-ia-companion@0.1.0 test
> vitest run

 Test Files  31 passed (31)
      Tests  164 passed (164)
```

---

## Diff Verification (Visual Parity)

```diff
diff --git a/src/renderer/styles/tokens.css b/src/renderer/styles/tokens.css
-/* ===== Design tokens (direction « Halo », clair + sombre) — repris de design.html tokens() ===== */
 :root{
+  /* ===== Primitifs : surfaces & couleurs ===== */
   --bg:#e7e1d4; --win:#f8f4ec; --panel:#f2ece1; --panel2:#ece5d8;
   --line:rgba(64,52,34,0.12); --text:#2e2a22; --muted:#6f6557; --faint:#a89d8b;
+
+  /* ===== Sémantiques : intentions ===== */
   --accent:oklch(0.52 0.072 46); --accentSoft:oklch(0.91 0.030 62); --onAccent:#faf6ee;
   --good:oklch(0.50 0.085 150); --goodSoft:oklch(0.91 0.045 150);
   --warn:oklch(0.53 0.135 34); --warnSoft:oklch(0.92 0.05 44);
   --nuance:oklch(0.58 0.10 74); --nuanceSoft:oklch(0.91 0.06 82);
+
+  /* ===== Échelle d'espacement (nouveau — travail neuf ; primitives existantes gardent leurs px d'origine pour parité) ===== */
+  --space-1:4px; --space-2:8px; --space-3:11px; --space-4:14px; --space-5:18px; --space-6:22px;
+
+  /* ===== Formes & typo ===== */
   --radius:14px; --radiusSm:10px; --shadow:0 14px 36px rgba(60,48,28,0.10);
   --display:'Hanken Grotesk',system-ui,sans-serif; --body:'Hanken Grotesk',system-ui,sans-serif;
   --mono:'JetBrains Mono',ui-monospace,monospace;
```

**Confirmation:** ✅ All existing token VALUES are byte-identical. Only comments/layers reorganized and new --space-* added. `body.dark` block untouched (verified via git diff).

---

## Gotchas de la passe

| Piège | Fix |
|-------|-----|
| String apostrophe in test name (`échelle d'espacement`) causing Vite parse error | Escaped apostrophe as `d\'espacement` in test string |
| Node 22 install failed in sandbox (read-only filesystem) | Used system Node v20.18.0 (sufficient for vitest 4 + ESM) |

---

## Décisions / restes

**Choix de conception validés :**
- Spacing scale: `4px, 8px, 11px, 14px, 18px, 22px` chosen per design system iteration (matches existing margin/padding patterns in codebase)
- Layer comments: French labels align with codebase convention (matching design.html tokens comments)
- Preservation of existing values: No touching color recipes, font names, or dark-mode overrides — ensures zero visual regression

**Différé :** Aucun — tâche complète. Prochain palier : Task 1.2 (Storybook foundation + story tests).
